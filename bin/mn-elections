#!/usr/bin/env node

/**
 * Main command line app.
 */


// Dependencies
require('dotenv').load();
const resultsFetch = require('../lib/results-fetch.js');
const metaFetch = require('../lib/meta-fetch.js');
const setupSupplement = require('../lib/setup-supplement.js');
const elections = require('../elections.json');

let e = elections['20131105'];
e.sos.user = process.env.SOS_FTP_USER;
e.sos.password = process.env.SOS_FTP_PASS;

// try {
//   metaFetch(e);
// }
// catch (e) {
//   console.error(e);
//   process.exit(1);
// }

try {
  resultsFetch(e);
}
catch (e) {
  console.error(e);
  process.exit(1);
}

// try {
//   setupSupplement();
// }
// catch (e) {
//   console.error(e);
//   process.exit(1);
// }
